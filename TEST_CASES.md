# 登录功能测试用例与执行结果

- 测试网站：`https://buka.org.nz`
- 执行日期：2026-09-11
- 执行环境：Windows、Python 3.10.9、pytest 9.1.1、Playwright 1.62.0、Chromium（无头模式）
- 汇总结果：7 条通过，0 条失败，用时 30.61 秒
- HTML 报告：`artifacts/report.html`
- 凭据保护：实际邮箱和密码仅从本地 `.env` 读取，本文档及测试代码均不记录真实密码

## TC-LOGIN-001 正确邮箱和正确密码登录成功

操作步骤：

1. 打开网站首页。
2. 在邮箱输入框填写 `.env` 中的管理员邮箱。
3. 在密码输入框填写 `.env` 中的管理员密码。
4. 点击“登录”。

预期结果：进入仪表盘；页面显示当前管理员邮箱、管理后台入口和退出登录按钮。

实际结果：**通过**。页面进入 `#/dashboard`，仪表盘标题、当前邮箱、管理后台入口和退出登录按钮均正常显示。

## TC-LOGIN-002 正确邮箱和错误密码提示失败

操作步骤：

1. 打开网站首页。
2. 填写正确的管理员邮箱。
3. 填写随机生成且与真实密码无关的错误密码。
4. 点击“登录”。

预期结果：登录失败；页面保留在登录区域并显示错误提示。

实际结果：**通过**。登录区域保持可见，`#auth-msg` 显示非空错误信息并带有错误状态样式，未进入仪表盘。

## TC-LOGIN-003 邮箱为空触发前端校验

操作步骤：

1. 打开网站首页。
2. 保持邮箱为空，填写正确密码。
3. 点击“登录”。

预期结果：浏览器阻止表单提交，邮箱字段提示必填，不发送登录接口请求。

实际结果：**通过**。邮箱字段 `valueMissing=true`、`valid=false`，显示浏览器校验信息并获得焦点；捕获到的 `/api/auth/login` 请求数为 0。

## TC-LOGIN-004 密码为空触发前端校验

操作步骤：

1. 打开网站首页。
2. 填写正确邮箱，保持密码为空。
3. 点击“登录”。

预期结果：浏览器阻止表单提交，密码字段提示必填，不发送登录接口请求。

实际结果：**通过**。密码字段 `valueMissing=true`、`valid=false`，显示浏览器校验信息并获得焦点；捕获到的 `/api/auth/login` 请求数为 0。

## TC-LOGIN-005 邮箱格式错误触发前端校验

操作步骤：

1. 打开网站首页。
2. 邮箱填写 `not-an-email`，密码填写正确密码。
3. 点击“登录”。

预期结果：浏览器提示邮箱格式错误并阻止提交，不发送登录接口请求。

实际结果：**通过**。邮箱字段 `typeMismatch=true`、`valid=false`，显示浏览器校验信息并获得焦点；捕获到的 `/api/auth/login` 请求数为 0。

## TC-LOGIN-006 登录后退出返回登录页

操作步骤：

1. 使用正确管理员凭据登录。
2. 确认仪表盘加载完成。
3. 点击“退出登录”。

预期结果：返回登录页，应用区域隐藏，本地登录令牌被清除。

实际结果：**通过**。登录表单重新显示，仪表盘应用区域隐藏，`localStorage` 中的 `buka_token` 已不存在。

## TC-LOGIN-007 未登录直接访问仪表盘被拦截

操作步骤：

1. 新建无登录状态的独立浏览器上下文。
2. 直接访问 `/#/dashboard`。

预期结果：不能看到仪表盘数据；显示登录页或跳回登录页。

实际结果：**通过**。登录表单可见，仪表盘应用区域隐藏，本地不存在登录令牌；未登录访问被拦截。

## 自动化代码对应关系

上述 7 条用例位于 `tests/test_login.py`。登录页操作和校验封装在
`pages/login_page.py`，仪表盘、退出和访问拦截操作封装在
`pages/dashboard_page.py`。
